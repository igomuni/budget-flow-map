- 検索で日本語入力のとき変換の確定(Enter)で選択されてしまう
- 検索して選択したノードを即時左パネルへ情報表示したい
- 検索、選択してノードを切り替わりをアニメーションしたい(GoogleMap風)
- 検索パネルは常時表示でいい
- 検索履歴も欲しい
- 左パネルと検索パネルは同じでいい左パネル閉じたときは検索パネル飲みが表示される感じ
- サイドパネルを閉じたときにメインビューのスクロールが変わらないようにしたい
- TopN設定ボタンを右下へ
  - 本当はTopNなくしたい**ただし、今やると全体が崩れるので実施は慎重に**
  - GoogleMapのようにZoom率に応じて表示されたRectを掘り下げられるようにしたい
  - ZoomOut時は事業と支出の表示閾値を1兆円、閾値以下はまとめて表示
  - ZoomInしていくと閾値が下がって解像度かあがっていくイメージ
- GoogleMapのZoomボタンのような作りへ
  - 右下に+,-ボタンのみ
  - +、-ボタンクリックでスライダーを表示/非表示の吹き出しが出てクリックすると縦スライダーが表示される
  - 左下のノード間隔調整等のコントロールもまとめて表示/非表示切り替えてもいいかも
- 支出先で`北海道`が2つある
- 支出先に表示される`所管府省`は複数ある場合もあるのでは
  - そもそも事業と支出先の基本情報は別の物なので左パネルの見せ方も違う方が良い
